[project]
name = "lctracker-backend"
version = "0.1.0"
description = "NeetCode problem tracking with spaced repetition"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]
